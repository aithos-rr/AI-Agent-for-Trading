# USD per 1M tokens. Committed pricing at 2026-06-13.
# pricing_snapshot in DB records the values used per invocation (PRE_PRD §11.7).
#
# Model names here follow the NATIVE API naming convention (gateway=direct).
# For gateway=openrouter, model names follow "provider/model" convention
# (e.g. "openai/gpt-4o"); add those keys here or map them to native keys before lookup.
# Final model selection (D1) is deferred to M7 seed_experiment.py; update then.
#
# Note: reasoning token pricing reflects chains-of-thought billed separately (o-series,
# DeepSeek-R1). For models without reasoning billing, set reasoning: 0.00.
models:
  openai-gpt-5.1:
    input: 1.25
    output: 10.00
    reasoning: 10.00
  anthropic-sonnet-4.7:
    input: 3.00
    output: 15.00
    reasoning: 0.00
  deepseek-r1-2026:
    input: 0.55
    output: 2.19
    reasoning: 2.19
  qwen-3-flagship:
    input: 0.80
    output: 3.20
    reasoning: 0.00
